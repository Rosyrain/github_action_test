name: Template Test
on:
  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "hello, world"
          export test=${{ secrets.TEST }}