name: Template Test
on:
  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "${{ github }}"
          echo "======================================================="
          echo "action_time=${{ github.action_time }}"